// Single source of truth for the user-facing app version (feedback GEN / "show
// version on login screen"). Bump this together with `version` in package.json
// on each release.
export const APP_VERSION = 'v1.0.0';
